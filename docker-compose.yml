services:
  postgres:
    image: postgres:15
    container_name: ccs_postgres
    restart: unless-stopped
    ports:
      # Loopback-only: the host can seed/inspect, but the DB is never reachable
      # from outside the machine (default credentials make 0.0.0.0 dangerous).
      - "127.0.0.1:5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-climate_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - ccs_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-climate_db}"]
      interval: 5s
      timeout: 5s
      retries: 10

  mongo:
    image: mongo:6.0
    container_name: ccs_mongo
    restart: unless-stopped
    ports:
      # Loopback-only — same rationale as postgres.
      - "127.0.0.1:27017:27017"
    volumes:
      - mongo_data:/data/db
      - ./database/mongo/init.js:/docker-entrypoint-initdb.d/init.js:ro
    networks:
      - ccs_net
    healthcheck:
      test: ["CMD", "mongosh", "--quiet", "--eval", "db.adminCommand('ping').ok"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s

  backend:
    build: ./backend
    container_name: ccs_backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      MONGO_URI: mongodb://mongo:27017
      POSTGRES_URI: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-climate_db}
      SECRET_KEY: ${SECRET_KEY:-}
      ALGORITHM: ${ALGORITHM:-HS256}
      ACCESS_TOKEN_EXPIRE_MINUTES: ${ACCESS_TOKEN_EXPIRE_MINUTES:-15}
      WATSON_API_KEY: ${WATSON_API_KEY:-}
      WATSON_URL: ${WATSON_URL:-}
      WATSON_ASSISTANT_ID: ${WATSON_ASSISTANT_ID:-}
      WATSON_EXTENSION_KEY: ${WATSON_EXTENSION_KEY:-}
    depends_on:
      postgres:
        condition: service_healthy
      mongo:
        condition: service_healthy
    volumes:
      - ./backend:/app
    networks:
      - ccs_net

  frontend:
    build: ./frontend
    container_name: ccs_frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      - backend
    networks:
      - ccs_net

networks:
  ccs_net:
    driver: bridge

volumes:
  postgres_data:
  mongo_data:
