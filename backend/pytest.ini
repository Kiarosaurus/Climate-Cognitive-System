[pytest]
testpaths = tests
asyncio_mode = auto
; Tests import the application as `app.*`. `python -m pytest` gets this for free
; (CWD on sys.path) but the bare `pytest` binary — used by CI and the README —
; does not; without this line collection dies with ModuleNotFoundError: app.
pythonpath = .
